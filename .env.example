# Anthropic API
ANTHROPIC_API_KEY=sk-ant-...

# Supabase Database
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...

# Slack Integration (Required for agent-human communication)
# See docs/SLACK_SETUP.md for detailed setup instructions
# Or README.md section "Slack Setup" for quick start

# Bot User OAuth Token - starts with xoxb-
# Found in: OAuth & Permissions > Bot User OAuth Token
# Used for: Sending messages, reading channels, posting notifications
SLACK_BOT_TOKEN=xoxb-...

# Signing Secret - used to verify requests from Slack
# Found in: Basic Information > App Credentials > Signing Secret
# Used for: Validating that requests actually come from Slack
SLACK_SIGNING_SECRET=...

# App-Level Token - starts with xapp-, requires Socket Mode enabled
# Found in: Basic Information > App-Level Tokens
# Used for: Establishing WebSocket connection for real-time events
# Requires scope: connections:write
SLACK_APP_TOKEN=xapp-...

# Channel name for agent notifications (without # prefix)
# Default: trafficcontrol
# Used for: Posting questions, blockers, and general notifications
SLACK_CHANNEL=trafficcontrol

# Channel ID (alternative to channel name, more reliable)
# Format: C followed by alphanumeric characters (e.g., C01234ABCDE)
# Found in: Right-click channel > View channel details > Copy ID
# Used for: Direct channel references, preferred over channel name
SLACK_CHANNEL_ID=C...

# Report channel for status reports (with # prefix)
# Default: #trafficcontrol (can be same as SLACK_CHANNEL)
# Used for: Morning/evening status reports, system metrics
SLACK_REPORT_CHANNEL=#trafficcontrol

# Capacity limits for concurrent agent sessions
# Conservative defaults to prevent runaway costs
# Recommended: opus <= 2, sonnet <= 5
OPUS_SESSION_LIMIT=1
SONNET_SESSION_LIMIT=2

# Reporter configuration
REPORT_MORNING_HOUR=8
REPORT_EVENING_HOUR=18
QUIET_HOURS_START=0
QUIET_HOURS_END=7
TIMEZONE=America/New_York

# Path overrides (optional)
# TC_LEARNINGS_PATH=./learnings
# TC_RETROSPECTIVES_PATH=./retrospectives
# TC_AGENTS_PATH=./agents.md

# Dashboard configuration
DASHBOARD_ENABLED=true
DASHBOARD_PORT=3000

# Logging configuration
# TC_LOG_LEVEL: DEBUG, INFO, WARN, ERROR (default: INFO)
TC_LOG_LEVEL=INFO
# TC_LOG_FORMAT: json, pretty (default: pretty)
TC_LOG_FORMAT=pretty
# TC_LOG_REDACT: Additional fields to redact (comma-separated)
# TC_LOG_REDACT=myCustomField,anotherField

# Slack Claude Relay configuration
# RELAY_TIMEOUT_MS: Timeout for CLI operations (default: 600000 = 10 min)
# RELAY_TIMEOUT_MS=600000
# RELAY_MODEL: Model to use - 'sonnet' or 'opus' (default: sonnet)
# RELAY_MODEL=sonnet
# RELAY_CLI_PATH: Path to claude CLI (default: claude)
# RELAY_CLI_PATH=claude
# RELAY_PROJECTS_DIR: Base directory for fuzzy project matching
# Enables partial project names like "trafficcontrol" -> "traffic-control"
# RELAY_PROJECTS_DIR=C:/Users/Eli/projects
